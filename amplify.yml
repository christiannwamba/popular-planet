version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 18.14.1
        - nvm use 18.14.1
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'